spring:
  datasource:
    url: "jdbc:h2:mem:test;INIT=create schema if not exists CAMP_REGISTRATIONS \\; create schema if not exists CAMP_EDITION" #jdbc:h2:file:~/test => local database | mem: in-memory
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console