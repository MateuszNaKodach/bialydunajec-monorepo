package org.bialydunajec.registrations.readmodel.statistics

import spock.lang.Specification

class CampRegistrationsEditionStatisticsUnitTest extends Specification {

/*
    def "camp registrations statistics should update after camp participant registration"() {
        given:
        String cottageId = "cottageId"
        def cottageSpace = 0
        def cottageCampParticipantsAmount = 0
        def stats = getSampleCottageStats(cottageId, cottageSpace, cottageCampParticipantsAmount)
        def campParticipantRegisteredEvent = getSampleParticipantRegisteredEvent(cottageId, Gender.MALE)

        when:
        stats.calculateWith(campParticipantRegisteredEvent)

        then:
        stats.campParticipantsAmount == 1
        stats.maleCampParticipantsAmount == 1
        stats.femaleCampParticipantsAmount == 0
    }
*/
}
