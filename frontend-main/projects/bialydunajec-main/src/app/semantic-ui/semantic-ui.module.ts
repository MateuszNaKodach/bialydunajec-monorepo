import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

const SEMANTIC_UI_MODULES = [];

@NgModule({
  imports: SEMANTIC_UI_MODULES,
  exports: SEMANTIC_UI_MODULES
})
export class SemanticUiModule { }
